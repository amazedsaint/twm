[project]
name = "trwm"
version = "0.1.0"
description = "A minimal transactional reversible world model kernel"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["trwm*"]

